const booleanType = () => {

    let pending: boolean;
    pending = true;

    // after some time in your execution

    pending = false;
};

// Don't use (avoid using): Boolean (B with uppercase)