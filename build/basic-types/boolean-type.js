"use strict";
/**
 * TypeScript boolean type
 *
 * The TypeScript boolean type has two values: true and false.
 * The boolean type is one of the primitive types in TypeScript
 */
const booleanType = () => {
    let pending;
    pending = true;
    // after some time in your execution
    pending = false;
};
// Don't use (avoid using): Boolean (B with uppercase)
/**
 * Summary
 * TypeScript 'boolean' type has two values 'true' and 'false'.
 * Use the 'boolean' keyword to declare boolean variables.
 * Do not use 'Boolean' type unless you have a good reason to do so. (Boolean, first uppercase letter in Boolean)
 */ 
